# H1 2027 candidates — skeleton

**Status: SKELETON, pending the Stage 4 persona experiment.** The stage prompt asks for
this list "from what the experiment revealed"; the measured runs are still blocked on
OPENROUTER_API_KEY (state file, Needs Luca 11), so the experiment-derived section is
empty by design. Everything else below is accumulated from H2 execution — deferred
scopes recorded in ADR amendments and learnings, plus DX questions the work surfaced.
Written 2026-07-20 (run 16); revise after the measured comparison note exists.

## From the persona experiment (PENDING — do not fill until measured runs exist)

- Verdict on the standing-team wedge: build out / iterate / drop. ← comparison note
- ADR-0036 orchestration decision follows the verdict (Stage 4 item 3).
- If the topology wins: persona layer v2 (self-improvement, persistent memory) per
  km/notes/2026-05-25-persona-team-on-oam.md's deferred layers.

## Deferred scopes recorded during H2 (each has rationale in its ADR amendment)

- **Heartbeat/zombie liveness layer** — ADR-0016 v1 deferred it; current detection is
  connection-level only. Needed before "agent hung but connected" is observable.
- **Traces + `$SYS` bridging + custom handler logging** — ADR-0048 v1 deferrals.
- **OTel export, caller-side usage accessor, sdk-ts usage-reporting parity** —
  ADR-0023 amendment deferrals.
- **A2A inbound gateway** (federation, not just card projection) — Stage 1 scoped it out.
- **SLA gating** — ADR-0006, untouched all H2.
- **add_mcp (inbound MCP → mesh)** — Phase 3 per ADR-0002's amendment.
- **Per-caller fast-fail subscription sharing** — ADR-0040 noted as future work "if
  callers get hot".

## DX questions surfaced by execution (candidates for shaping)

- **Eager registration mode** — run-2 finding (lazy registration bites every
  multi-connection scenario); also the persona experiment's proposed debate task, so
  the experiment may answer it for free.
- **Accept `AsyncIterator[Chunk]` annotations in `inspect_handler`** — run-1 learning:
  the ADR-0031 streamer convention fights strict typing; typed user code can't check
  clean without it.
- **Graceful per-agent teardown for ungated agents** — the ADR-0055 task-dispatch
  machinery exists; ungated agents still hard-stop.

## Distribution debt carried out of H2 (blocked items, not new work)

- Launch execution: demo recording, post + Show HN publishing, docs URL decision,
  npm + PyPI releases — all sitting in Needs Luca items 1–8.
- Community surface: issues/Discord/contributing story — explicitly out of H2 scope,
  becomes real the moment launch happens.
